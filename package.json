{
  "type": "module",
  "scripts": {
    "start": "go run main.go",
    "stop": "kill-port 3000 > /dev/null 2>&1 || true",
    "test:only": "curl -fv http://localhost:3000",
    "test": "./start-and-test.sh"
  },
  "devDependencies": {
    "kill-port-process": "^3.2.1",
    "wait-port": "^1.1.0"
  }
}
